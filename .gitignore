# Generated trace outputs from local fleet_sim runs (ADR-009 trace files
# are outputs, never committed fixtures; golden traces would get explicit
# names and be force-added if ever needed).
/trace*.jsonl
